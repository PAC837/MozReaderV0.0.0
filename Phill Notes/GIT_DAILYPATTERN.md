8. Daily Pattern (TL;DR)

git checkout main

git pull

git checkout -b feature/<short-name>

Work in Unity

git status

git add Assets ProjectSettings Packages

git commit -m "message"

git push -u origin feature/<short-name> (first time)
then just git push on later updates

git checkout main to get back to main

git branch -d feature

git branch -D feature (if it refuses to die because its not merged)



git pull

git checkout -b feature to make a new task